(ns nicheware.platform.utilities.common.hello-test
  (:require-macros [cljs.test :refer (is deftest testing)])
  (:require [cljs.test]))

(deftest example-passing-test
  (println "an example-passing-test")
  (is (= 1 1)))
